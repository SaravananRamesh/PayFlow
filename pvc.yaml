apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
spec:
  accessModes:
    - ReadWriteOnce       # Fine here — only the postgres pod writes to this
  resources:
    requests:
      storage: 1Gi
  storageClassName: do-block-storage
